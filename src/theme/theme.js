import { extendTheme } from '@chakra-ui/react';

const theme = {
     inputBgColors:{
        background: "linear - gradient(90deg, rgba(254, 254, 254, 0.56) - 3.22 %, rgba(255, 255, 255, 0.16) 95.7 %)",
        backdropFilter: "blur(25px)"
     }
}

export default extendTheme(theme);